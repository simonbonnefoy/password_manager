from setuptools import setup, find_packages

setup(
   name='password_manager',
   version='0.0.1',
   description='A basic password manager',
   author='Simon Bonnefoy',
   author_email='',
   packages=find_packages()
)
